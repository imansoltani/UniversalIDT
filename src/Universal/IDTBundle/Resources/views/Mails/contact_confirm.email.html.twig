{% block subject %}{{ 'emails.title.contact_confirm'|trans({},'application') }}{% endblock %}

{% block body %}
    {{ 'emails.hello'|trans({'%first_name%':name},'application') }}
    <br /><br />
    {{ 'emails.email_sent'|trans({},'application') }}
    <br /><br />
    {{ 'emails.greetings'|trans({},'application')|raw }}
{% endblock %}