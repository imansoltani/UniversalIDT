{% extends '@UniversalIDT/layout.html.twig' %}

{% block subtitle %}
    {{ 'settings.email.title'|trans({},'application') }}
{% endblock subtitle %}

{% block content %}
    <div class="content-container clearfix">
        <div class="main-content clearfix"><!-- MAIN CONTENT -->
            <h2 class="section-title">{{ 'settings.email.subtitle'|trans({},'application') }}</h2>
            <div class="user-panel clearfix"><!-- USER PANEL -->
                <ul class="nav nav-tabs"><!-- TAB LIST -->
                    <li><a href="{{ path('fos_user_profile_edit') }}"><i class="fa fa-user"></i> {{ 'settings.profile.subtitle'|trans({},'application') }}</a></li>
                    <li class="active"><a href="{{ path('user_profile_email') }}"><i class="fa fa-envelope"></i> {{ 'settings.email.subtitle'|trans({},'application') }}</a></li>
                    <li><a href="{{ path('fos_user_change_password') }}"><i class="fa fa-key"></i> {{ 'settings.password.subtitle'|trans({},'application') }}</a></li>
                    <li><a href="{{ path('user_profile_notification') }}"><i class="fa fa-exclamation-circle"></i> {{ 'settings.notifications.subtitle'|trans({},'application') }}</a></li>
                </ul>
                <div class="tab-content clearfix">
                    <div class="tab-pane active">
                        <div class="current-email-address clearfix">
                            <p>{{ 'settings.email.paragraph'|trans({},'application') }} <strong>{{ email }}</strong></p>
                        </div>
                        {{ form_start(form, {'attr': {'class': 'email-setting-form form-horizontal'}}) }}
                            <div class="form-group">
                                {{ form_label(form.newEmail, "", {'label_attr': {'class': 'col-md-3'} }) }}
                                <div class="col-md-6">
                                    <div class="input-group">
                                        <span class="input-group-addon"><i class="fa fa-envelope"></i></span>
                                        {{ form_widget(form.newEmail, {'attr': {'class': 'form-control'}}) }}
                                        {{ form_errors(form.newEmail) }}
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                {{ form_label(form.current_password, "", {'label_attr': {'class': 'col-md-3'} }) }}
                                <div class="col-md-6">
                                    <div class="input-group">
                                        <span class="input-group-addon"><i class="fa fa-key"></i></span>
                                        {{ form_widget(form.current_password, {'attr': {'class': 'form-control'}}) }}
                                        {{ form_errors(form.current_password) }}
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-md-6 col-md-offset-3">
                                    {{ form_widget(form.submit, {'attr': {'class': 'btn btn-primary'}}) }}
                                </div>
                            </div>
                        {{ form_errors(form) }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}